# Mongodb-collection-data-schema--validation
Accept a directory of schema files where each schema files for a collection should be name as the collection name and the directory name is the database name.
